{{-- Phase 5 replaces this with the Filament panel. It exists now so the
     authorisation middleware has something real to protect, and so the
     direct-URL tests in Phase 4 assert against a route that resolves. --}}
<x-layouts::form title="Admin">
    <h1 class="t-h1 text-forest-800">Admin</h1>
    <p class="t-body text-ink mt-5">The back office arrives in Phase 5.</p>
</x-layouts::form>
